# Messenger Application 📱

Полнофункциональное мобильное приложение-мессенджер для Android с поддержкой текстовых сообщений, фото и видео.

## 🎨 Особенности

### Аутентификация
- ✅ Регистрация новых пользователей
- ✅ Вход в существующий аккаунт
- ✅ Безопасное хранение данных в Firebase Authentication

### Мессенджер
- ✅ Отправка текстовых сообщений
- ✅ Отправка фотографий
- ✅ Отправка видео
- ✅ Real-time синхронизация
- ✅ Список всех пользователей

### Дизайн
- 🌙 Темная тема по умолчанию
- 🎨 Пользовательская цветовая палитра:
  - Основной цвет (золотой): `#df9f1f`
  - Светлый золотой: `#eecd8a`
  - Темный фон: `#1c1404`
  - Серый: `#848483`
  - Светло-серый: `#adacab`
  - Темно-коричневый: `#503c32`

## 🛠 Технический стек

### Frontend
- **Язык**: Kotlin
- **Фреймворк**: Android SDK / AndroidX
- **UI**: Material Design Components
- **Binding**: View Binding

### Backend
- **Firebase**:
  - Firebase Authentication (аутентификация)
  - Firebase Realtime Database (хранение сообщений)
  - Firebase Storage (хранение медиа-файлов)

### Библиотеки
- **Coroutines**: Асинхронное программирование
- **Glide**: Загрузка и кэширование изображений
- **RecyclerView**: Списки сообщений и пользователей

## 📋 Структура проекта

```
app/
├── src/main/
│   ├── kotlin/com/example/messenger/
│   │   ├── ui/
│   │   │   ├── AuthActivity.kt          # Экран входа/регистрации
│   │   │   ├── ChatActivity.kt          # Экран чата
│   │   │   └── adapters/
│   │   │       ├── MessageAdapter.kt    # Адаптер для сообщений
│   │   │       └── UsersAdapter.kt      # Адаптер для пользователей
│   │   ├── firebase/
│   │   │   └── FirebaseManager.kt       # Управление Firebase операциями
│   │   └── models/
│   │       ├── User.kt                  # Модель пользователя
│   │       └── Message.kt               # Модель сообщения
│   ├── res/
│   │   ├── layout/
│   │   │   ├── activity_auth.xml        # Макет входа
│   │   │   ├── activity_chat.xml        # Макет чата
│   │   │   ├── item_message.xml         # Элемент сообщения
│   │   │   └── item_user.xml            # Элемент пользователя
│   │   ├── values/
│   │   │   ├── colors.xml               # Цветовая палитра
│   │   │   ├── strings.xml              # Строки (русский язык)
│   │   │   └── themes.xml               # Темная тема
│   │   └── drawable/
│   │       └── message_background.xml   # Фон сообщений
│   └── AndroidManifest.xml              # Манифест приложения
├── build.gradle.kts                     # Конфигурация зависимостей
└── README.md                            # Документация
```

## 🚀 Быстрый старт

### Предварительные требования
- Android Studio (последняя версия)
- JDK 11+
- Firebase проект

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/Artem2335/app.git
cd app
```

2. **Откройте проект в Android Studio**
```bash
android-studio .
```

3. **Настройте Firebase**
   - Перейдите на [Firebase Console](https://console.firebase.google.com/)
   - Создайте новый проект или выберите существующий
   - Добавьте Android приложение в проект
   - Скачайте `google-services.json`
   - Поместите файл в папку `app/`

4. **Установите Firebase Rules**

   **Realtime Database Rules:**
   ```json
   {
     "rules": {
       "users": {
         "$uid": {
           ".read": "$uid === auth.uid || !auth.exists()",
           ".write": "$uid === auth.uid"
         }
       },
       "messages": {
         "$messageId": {
           ".read": true,
           ".write": "auth.uid === root.child('messages').child($messageId).child('senderId').val() && newData.child('senderId').val() === auth.uid"
         }
       }
     }
   }
   ```

   **Storage Rules:**
   ```
   rules_version = '2';
   service firebase.storage {
     match /b/{bucket}/o {
       match /users/{userId}/{allPaths=**} {
         allow read: if request.auth.uid != null;
         allow write: if request.auth.uid == userId;
       }
     }
   }
   ```

5. **Соберите и запустите приложение**
   - Нажмите `Run` → `Run 'app'` в Android Studio
   - Или используйте терминал:
   ```bash
   ./gradlew installDebug
   ```

## 🔐 Безопасность

- Все пароли хранятся в Firebase Authentication
- Данные пользователей защищены Firebase Rules
- Медиа-файлы хранятся в Firebase Storage с проверкой прав доступа
- Используется HTTPS для всех соединений

## 📸 Скриншоты

### Экран входа
- Темная тема с золотым акцентом
- Поля для email и пароля
- Кнопка переключения между входом и регистрацией

### Экран чата
- Список пользователей
- Окно чата с сообщениями
- Кнопки для отправки фото и видео
- Кнопка выхода

## 🐛 Известные ограничения

- Сообщения не удаляются (только отправляются)
- Нет редактирования отправленных сообщений
- Нет группового чата (только 1 на 1)
- Нет типа сообщений «печатает..."

## 📱 Поддерживаемые версии Android

- Минимальная версия: API 24 (Android 7.0)
- Целевая версия: API 34 (Android 14)

## 🤝 Вклады

Приветствуются pull requests! Для больших изменений сначала откройте issue для обсуждения изменений.

## 📄 Лицензия

Этот проект лицензирован под MIT License - смотрите LICENSE файл для подробностей.

## 👤 Автор

**Artem** - [@Artem2335](https://github.com/Artem2335)

---

**Сделано с ❤️ для Android разработчиков**
