# 🔧 Руководство по настройке Messenger приложения

## Шаг 1: Подготовка Firebase проекта

### Создание Firebase проекта

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **"Создать проект"** (Create Project)
3. Введите имя проекта (например, "Messenger")
4. Согласитесь с условиями и создайте проект
5. Дождитесь завершения инициализации проекта

### Добавление Android приложения

1. В Firebase Console нажмите на иконку Android ( **</>** )
2. Введите детали Android приложения:
   - **Имя пакета**: `com.example.messenger`
   - **Кличка приложения** (опционально): Messenger
3. Нажмите **"Зарегистрировать приложение"**
4. **Скачайте `google-services.json`** файл
5. Поместите файл в папку `app/` вашего проекта

## Шаг 2: Настройка Firebase Services

### Включение Authentication

1. Перейдите в **Authentication** → **Начать**
2. Нажмите на **Email/Password** в разделе "Способы входа"
3. Включите **Email/Password** аутентификацию
4. Нажмите **Сохранить**

### Создание Realtime Database

1. Перейдите в **Realtime Database** → **Создать базу данных**
2. Выберите регион (рекомендуется ближайший к вам)
3. Выберите **Начать в режиме тестирования** (для разработки)
4. Нажмите **Включить**
5. Перейдите в **Правила** и вставьте следующие правила:

```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid || !auth.exists()",
        ".write": "$uid === auth.uid"
      }
    },
    "messages": {
      "$messageId": {
        ".read": true,
        ".write": "auth.uid === root.child('messages').child($messageId).child('senderId').val() && newData.child('senderId').val() === auth.uid"
      }
    }
  }
}
```

6. Нажмите **Опубликовать**

### Настройка Cloud Storage

1. Перейдите в **Storage** → **Начать**
2. Выберите регион (тот же, что и для Database)
3. Выберите **Начать в режиме тестирования**
4. Нажмите **Сделано**
5. Перейдите в **Правила** и вставьте:

```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      allow read: if request.auth.uid != null;
      allow write: if request.auth.uid == userId;
    }
  }
}
```

6. Нажмите **Опубликовать**

## Шаг 3: Подготовка Android проекта

### Открытие проекта в Android Studio

1. Откройте Android Studio
2. Выберите **File** → **Open**
3. Выберите папку репозитория `app`
4. Дождитесь загрузки и синхронизации Gradle

### Добавление google-services.json

1. Убедитесь, что `google-services.json` находится в папке `app/`
2. Файл должен быть в корне папки `app`, НЕ в `app/src`
3. Структура должна быть:
   ```
   app/
   ├── google-services.json    ← Здесь!
   ├── src/
   ├── build.gradle.kts
   └── ...
   ```

## Шаг 4: Сборка и запуск приложения

### Синхронизация Gradle

1. Нажмите **File** → **Sync Now**
2. Дождитесь завершения синхронизации
3. Проверьте отсутствие ошибок в logcat

### Запуск на эмуляторе или устройстве

1. Выберите или создайте Android эмулятор (минимум API 24)
2. Или подключите реальное Android устройство через USB
3. Нажмите **Run** → **Run 'app'** (или нажмите **Shift+F10**)
4. Выберите целевое устройство
5. Дождитесь установки и запуска приложения

### Альтернативный запуск через терминал

```bash
# Установка и запуск на подключенном устройстве
./gradlew installDebug

# Или с запуском
./gradlew installDebugAndRun
```

## Шаг 5: Тестирование приложения

### Первый запуск

1. Вы должны увидеть экран входа с темной темой и золотыми акцентами
2. Нажмите **"Зарегистрироваться"** для создания нового аккаунта
3. Введите:
   - Email (например: test@example.com)
   - Имя пользователя (минимум 3 символа)
   - Пароль (минимум 6 символов)
   - Подтверждение пароля
4. Нажмите **Зарегистрироваться**

### Создание второго аккаунта

1. После регистрации вернитесь на экран входа
2. Нажмите **Войти** для переключения в режим входа
3. Введите данные первого аккаунта
4. Нажмите **Войти**
5. Вы должны увидеть экран чата

### Тестирование чата

1. На эмуляторе откройте второе окно терминала
2. Создайте второй эмулятор или используйте другое устройство
3. Установите приложение на второе устройство
4. Создайте второй аккаунт на втором устройстве
5. На первом устройстве нажмите на пользователя из списка
6. Отправляйте сообщения между устройствами

### Тестирование медиа

1. В чате нажмите **"Фото"** или **"Видео"**
2. Выберите файл из хранилища эмулятора
3. Сообщение с медиа должно отправиться и отобразиться в чате

## 🐛 Решение распространенных проблем

### Проблема: "Failed to resolve com.google.gms:google-services"

**Решение:**
1. Убедитесь, что `google-services.json` находится в папке `app/`
2. Выполните **File** → **Sync Now**
3. Очистите проект: **Build** → **Clean Project**
4. Пересоберите: **Build** → **Rebuild Project**

### Проблема: "Authentication error" при входе

**Решение:**
1. Проверьте интернет-соединение на устройстве/эмуляторе
2. Убедитесь, что Email/Password аутентификация включена в Firebase
3. Проверьте правильность введенного email и пароля
4. Проверьте правила Realtime Database (не должны блокировать запросы)

### Проблема: "Database reference error"

**Решение:**
1. Перейдите в Firebase Console → Realtime Database
2. Убедитесь, что база данных создана и активна
3. Проверьте правила доступа
4. Попробуйте перезагрузить приложение

### Проблема: "Storage upload failed"

**Решение:**
1. Убедитесь, что Cloud Storage создан
2. Проверьте правила Storage (должны разрешать запись)
3. Проверьте разрешения на доступ к файлам:
   - AndroidManifest.xml должен содержать `READ_EXTERNAL_STORAGE`
   - На Android 6.0+ запросите разрешения runtime

### Проблема: Сообщения не синхронизируются

**Решение:**
1. Проверьте подключение к интернету
2. Проверьте правила Realtime Database (должны разрешать чтение/запись)
3. Убедитесь, что пользователи созданы в Firebase Authentication
4. Очистите данные приложения и переустановите его

## 📱 Требования к устройству

- **Минимальная версия Android**: 7.0 (API 24)
- **Целевая версия Android**: 14 (API 34)
- **Оперативная память**: минимум 2 GB
- **Место на диске**: минимум 50 MB для установки приложения
- **Интернет**: требуется подключение для работы Firebase

## 🔑 Ключевые особенности реализации

### Архитектура
- **MVVM pattern** с использованием Kotlin Coroutines
- **Firebase** для backend и хранения данных
- **View Binding** для работы с UI элементами
- **RecyclerView** для списков

### Безопасность
- Firebase Authentication для защиты пользовательских данных
- Firebase Rules для контроля доступа к базе данных
- Шифрование данных при передаче (HTTPS/SSL)

### Производительность
- Асинхронная загрузка данных с Coroutines
- Кэширование изображений с Glide
- Оптимизированные запросы к базе данных

## 📚 Дополнительные ресурсы

- [Firebase Documentation](https://firebase.google.com/docs)
- [Android Developers](https://developer.android.com/)
- [Kotlin Documentation](https://kotlinlang.org/docs/home.html)
- [Material Design](https://material.io/design)

## 💡 Советы разработки

1. **Используйте логирование**: добавьте `Log.d()` для отладки
2. **Тестируйте на реальном устройстве**: эмулятор может работать не так же
3. **Проверяйте Firebase Rules**: они критичны для безопасности
4. **Мониторьте использование Firebase**: бесплатный план имеет лимиты
5. **Обновляйте зависимости**: регулярно проверяйте новые версии библиотек

---

**Если у вас есть вопросы, создайте Issue в репозитории! 🚀**
